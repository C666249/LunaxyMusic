# Lunaxy Music V89 · Engine Insight UX

## 目标
让矩阵从“正文里的工程面板”变成上下文式解释层。正常页面始终以音乐、搜索结果和推荐内容为主。

## 统一交互
- 右上角 SCAN 引擎键：轻点 = 快速「引擎洞察」Bottom Sheet。
- 长按同一按键：直接进入完整矩阵。
- Bottom Sheet 内保留“完整音乐画像 / 完整搜索矩阵”显式入口，避免只有隐藏手势。

## 个性化
- 首页推荐横向卡片仅保留每日推荐、私人电台、天气电台，不再常驻“个性化矩阵”第四卡。
- 每日推荐 / 私人电台详情页右上角出现上下文引擎键。
- 快速洞察只显示新发现、ListenBrainz、熟悉延伸、再发现，以及少量推荐原因；完整页面产品化命名为“我的音乐画像”。
- 首页长按“每日推荐”或“私人电台”仍可直达完整音乐画像。

## 搜索
- 搜索页右上角使用同一 SCAN 引擎键。
- 来源筛选行移除“矩阵”文字 Chip，避免把分析功能伪装成第五个来源。
- 快速洞察展示首屏耗时、意图、参与来源、当前最快来源、合并结果数量和歌词链状态。
- 长按或 Bottom Sheet 按钮进入完整搜索矩阵。

## 安全边界
V89 不修改 PlaybackService、SourceCoordinator、PlaybackUrlStore、ImageLoader、LyricCacheStore，不改推荐召回/排序与 Search Engine V2 网络调度。
## 视觉升级（本轮追加验收）
- “我的音乐画像”不再使用固定 250dp 宽度的普通进度条，统一改成自适应宽度的轨道式 Insight Rail：四分位刻度、渐进填充、终点节点与 320ms 克制进场动画。
- 学习成熟度改为“大数字 + 自适应轨道 + 认识中/稳定画像”层级，不再把百分比塞在一行小字里。
- Session 四项统计改为 2×2 有色玻璃卡片，避免窄屏四列拥挤。
- 当前最强兴趣增加四段时间光谱：Session / 7天 / 30天 / 180天用不同轨道色表达强弱。
- “画像”Tab 每个歌手的四条时间轨道全部响应容器实际宽度，不再用固定 dp 截断；负向信号仍使用粉色反向轨道。
- “推荐”Tab 增加熟悉 ↔ 新发现平衡轨，四项来源统计继续使用 2×2 自适应卡片。
- 视觉升级只读现有 Personalization 数据，不改变 Taste Profile、推荐召回/排序、ListenBrainz 权重或播放逻辑。

