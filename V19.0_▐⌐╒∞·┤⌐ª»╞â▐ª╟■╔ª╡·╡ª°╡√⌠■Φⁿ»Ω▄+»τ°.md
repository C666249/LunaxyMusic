# XingyuMusic V19.0：歌词手势硬隔离

## 本轮问题
V18 虽然给 `lyricScroll` 设置了 `OnTouchListener`，但每一行歌词本身是可点击 View。Android 触摸分发时，可点击歌词行可能先成为触摸目标，导致父级歌词 ScrollView 的监听器拿不到最早的 DOWN；外层播放页 ScrollView 随后就有机会截获纵向拖动，所以表现为“歌词很难滑，页面反而跟着滑”。

## V19 修复
- 新增 `LyricGestureScrollView`：在 `dispatchTouchEvent()` 层、歌词行收到事件之前观察并锁定手势。
- 外层 `SwipeAwareScrollView` 新增歌词卡片排除区：手指 DOWN 在歌词框内时，外层页面整个手势序列禁止截获；DOWN 在框外时，页面照常上下滑。
- 因而区域规则变成确定的：**歌词框内 = 只滚歌词；歌词框外 = 只滚播放页**。
- 已经出现虚线选中态后，可以直接再次按住任意歌词上下滑；一旦超过系统 touch-slop，旧虚线选中态立即取消并重新进入定位模式。
- 普通轻点仍会传给歌词行：如果点的是虚线选中的句子，继续执行原有 seekTo 跳转。

## 播放链保护
本轮没有修改 `data/**`、`playback/**`、Huibq JS、AndroidManifest。详见 `changes/PLAYBACK_CORE_GUARD_V19.txt`。
