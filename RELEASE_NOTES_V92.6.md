# V92.6 — 唱片翻面歌词、单唱片与快速滚动

基于 V92.5.1，用户提供的旧源码目录保持不变。

## 使用方式

- 常态只有当前唱片；左右滑动最多切换上一/下一首，跟手位移、淡入淡出并在松手后落位。沿用上一版灵敏度设置。
- 点击唱片，以 420ms 的 Y 轴翻转露出背面歌词。歌词仍可独立滚动和定位；左上角返回图标或系统返回翻回正面。
- 长按唱片或底部播放列表按钮展开/收起多唱片浏览。展开后保留连续跨多首浏览。普通点击播放列表按钮仍打开列表。退出播放详情后恢复单唱片。
- 播放列表和手动排序列表保持原生拖动，超过 1200dp/s 的甩动逐步降低摩擦，到 5000dp/s 达到最高增益；提供原生快速滚动条。
- 拖动排序到边缘时，根据深入边缘区域的程度和停留时间逐步加速，步长 4–36dp / 42ms；退出边缘即停止，不修改播放队列提交时机。
- 桌面歌词改为单遍原生文字绘制，去掉重复描边绘制、强制纹理层以及每句淡入位移；增加半透明深色底，保留配色、字号及长句跑马灯设置。

## 参考与范围

Android 官方提供按甩动速度处理惯性及快速滚动条的接口，本版沿用原生列表，不引入第二套手势惯性：
https://developer.android.com/reference/android/widget/AbsListView

Spotify 与 Apple 官方说明可作为同步歌词交互参考，但未公开本任务所需的私有滚动参数或桌面浮层文字渲染算法，本版不声称复刻这些算法：
https://support.spotify.com/ie/article/lyrics/
https://support.apple.com/en-gb/108960

播放解析、下载、歌词解析、封面缓存算法保持不变。桌面文字清晰度最终仍需在用户手机的系统浮层合成环境验收。

Stable：com.xingyu.music / 92.6.0 / 934。
Beta：com.xingyu.music.beta1 / 92.6.0-beta32 / 935，独立 β 图标。
APK 为 debug 签名安装包；完整源码可在 Android Studio 打开。测试记录见 dev-logs/V92.6-validation.md。
