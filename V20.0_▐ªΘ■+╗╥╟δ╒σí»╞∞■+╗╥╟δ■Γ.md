# XingyuMusic V20.0

## 1. QQ 歌词：独立元数据链，不碰音频播放链

V20 对纯 QQ 歌曲按以下顺序获取歌词：

1. 如果歌曲本身已有网易 variant：直接走原网易歌词。
2. 纯 QQ 歌曲先检查本地 `QQ songmid -> 网易 songId` 映射。
3. 没有映射时，仅用「歌名 + 主歌手 + 时长」在网易做保守匹配；歌名/歌手必须一致，时长偏差过大直接拒绝，避免错配 Live/Remix。
4. 找到网易同曲后只借用网易 LRC，并把映射永久保存。
5. 网易没有可信同曲时，再使用独立 `QQLyricApi` 请求 QQ 自己的 LRC 作为兜底。
6. 成功歌词按歌曲永久本地缓存，之后打开播放页不必重复获取。

这套链路不会给 Song 新增/重排播放 variant，也不会写 SourceHealthStore、PlaybackUrlStore 或调用 Huibq/QDY。

## 2. 歌词高亮回到逐句

V18/V19 的 YRC/插值逐字显示在部分歌曲上存在「字比人声慢」的问题。V20 改回 LRC 时间戳逐句高亮：当前句整句冰蓝渐变亮起，下一句到点再切换。手动滚动、中心定位线、虚线选中、点击跳转仍保留。

## 3. 首页推荐卡

原「收藏 / 歌单」两个首页卡片删除（底部 Tab 已有对应入口），原位置改为：

- 每日推荐：基于收藏权重最高、歌单其次、最近播放再次的本地口味画像，取最常听歌手，用现有网易/QQ“元数据搜索”发现候选；不请求 Huibq/QDY。每日结果本地缓存，同一天再次点击直接复用。
- 私人电台：完全本地，把收藏、歌单、最近播放按权重混排成播放队列，不需要任何额外网络 API。

## 4. 播放缓存审计

V19/V20 原本就满足「播成功一次后优先复用」：

- Media3 READY 后，`PlaybackUrlStore` 才保存 URL。
- 下次播放同平台 ID + 同音质时，`SourceCoordinator` 先读缓存，不调用 Huibq/QDY。
- 缓存 URL 若被 CDN 拒绝/过期，播放器才删除缓存并刷新一次音源。
- V20 没有修改这一逻辑，也没有清空 `xingyu_playback_urls_v8` 或 `source_health_v8`。

QDY 即使缓存命中，也可能因为其返回的最终 CDN/中转 URL 首包慢而比 Huibq 慢；若该 URL 本身短时有效，过期后会触发一次刷新。这和“每次都请求 QDY”不是一回事。

## 5. 版本

- versionCode: 23
- versionName: 20.0.0
