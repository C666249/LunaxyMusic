--- /mnt/data/v30baseline/LunaxyMusic-V30/app/src/main/java/com/xingyu/music/MainActivity.java	2026-08-10 12:32:54.000000000 +0000
+++ /mnt/data/v31work/LunaxyMusic-V30/app/src/main/java/com/xingyu/music/MainActivity.java	2026-08-10 13:01:31.884553188 +0000
@@ -92,7 +92,7 @@
 import java.util.regex.Pattern;
 
 /**
- * Lunaxy Music V30.0 – full-line lyric gradient preview + shorter manual lyric selection hold.
+ * Lunaxy Music V31.0 – lower lyric viewport entry point + gesture boundary follows the moved panel.
  *
  * V30 keeps the proven audio-resolution/playback core frozen. Tapping a normal row in a saved playlist
  * still replaces the current queue with the entire playlist and starts at that row. Tapping a result
@@ -3783,10 +3783,15 @@
         lyricPanel.addView(lyricPalette, paletteP);
 
         LinearLayout.LayoutParams lyricP = Ui.lp(-1, Ui.dp(this, 258));
-        lyricP.topMargin = Ui.dp(this, 20);
+        // V31: keep the lyric card fully below the initial transport area instead of letting
+        // a thin strip peek at the bottom of the now-playing page. This removes the visual
+        // "flash past" feeling when the internally auto-scrolling lyrics are barely visible.
+        lyricP.topMargin = Ui.dp(this, 44);
         content.addView(lyricPanel, lyricP);
         // Hard boundary: any gesture whose DOWN lands inside the lyric card belongs to
-        // LyricGestureScrollView. The outer page only scrolls when DOWN starts outside.
+        // LyricGestureScrollView. The exclusion rectangle is resolved from lyricPanel's live
+        // on-screen bounds on every DOWN, so moving the panel down also moves the lyric
+        // manual-scroll hit area automatically; the old location does not remain interactive.
         scroll.setGestureExclusionView(lyricPanel);
 
         nowSeek.setOnSeekBarChangeListener(new SeekBar.OnSeekBarChangeListener() {
--- /mnt/data/v30baseline/LunaxyMusic-V30/app/build.gradle.kts	2026-08-10 12:30:47.000000000 +0000
+++ /mnt/data/v31work/LunaxyMusic-V30/app/build.gradle.kts	2026-08-10 13:01:31.885002926 +0000
@@ -10,8 +10,8 @@
         applicationId = "com.xingyu.music"
         minSdk = 26
         targetSdk = 36
-        versionCode = 33
-        versionName = "30.0.0"
+        versionCode = 34
+        versionName = "31.0.0"
     }
 
     buildTypes {
--- /mnt/data/v30baseline/LunaxyMusic-V30/BUILD_APK.bat	2026-08-10 12:33:53.000000000 +0000
+++ /mnt/data/v31work/LunaxyMusic-V30/BUILD_APK.bat	2026-08-10 13:01:31.885062135 +0000
@@ -2,7 +2,7 @@
 setlocal
 cd /d "%~dp0"
 echo ==========================================
-echo   LunaxyMusic V30 - Debug APK Build
+echo   LunaxyMusic V31 - Debug APK Build
 echo ==========================================
 call gradlew.bat :app:assembleDebug
 if errorlevel 1 (
@@ -12,7 +12,7 @@
   exit /b 1
 )
 if not exist dist mkdir dist
-copy /Y "app\build\outputs\apk\debug\app-debug.apk" "dist\LunaxyMusic-V30-debug.apk" >nul
+copy /Y "app\build\outputs\apk\debug\app-debug.apk" "dist\LunaxyMusic-V31-debug.apk" >nul
 echo.
-echo [OK] APK: %CD%\dist\LunaxyMusic-V30-debug.apk
+echo [OK] APK: %CD%\dist\LunaxyMusic-V31-debug.apk
 pause
--- "/mnt/data/v30baseline/LunaxyMusic-V30/README_\351\252\214\346\224\266\350\257\264\346\230\216.txt"	2026-08-10 12:33:53.000000000 +0000
+++ "/mnt/data/v31work/LunaxyMusic-V30/README_\351\252\214\346\224\266\350\257\264\346\230\216.txt"	2026-08-10 13:02:29.247786321 +0000
@@ -1,3 +1,18 @@
+LunaxyMusic V31.0 — 当前 Android Studio 完整工程
+
+【本版快速验收】
+1. 播放详情页顶部初始位置：歌词卡片比 V30 下移 24dp，不再只在底部露出一条会自动滚动的歌词边缘。
+2. 向下滑页面后：歌词卡片正常完整出现；卡片内上下滑只滚歌词，卡片外上下滑滚页面。
+3. 歌词手动判定区域跟随 lyricPanel 的实时屏幕位置，不使用旧坐标或写死范围。
+4. V30 的整句歌词渐变、可视化选色器、2 秒虚线选中、大歌单虚拟化均保留。
+5. 播放/音源核心没有修改，详见 changes\PLAYBACK_CORE_GUARD_V31.txt。
+6. V30→V31 源码差异见 changes\V30-to-V31_LYRIC_PANEL_POSITION.patch。
+7. BUILD_APK.bat 成功构建后输出 dist\LunaxyMusic-V31-debug.apk。
+
+----------------------------------------
+以下保留 V30 及旧版本验收记录，供回退/排查历史问题使用。
+----------------------------------------
+
 LunaxyMusic V30.0 — 当前 Android Studio 完整工程
 
 【本版快速验收】
