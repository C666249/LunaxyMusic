diff -ruN /mnt/data/v39diff/old/BUILD_APK.bat /mnt/data/v39diff/new/BUILD_APK.bat
--- /mnt/data/v39diff/old/BUILD_APK.bat	2026-08-11 05:07:04.779059620 +0000
+++ /mnt/data/v39diff/new/BUILD_APK.bat	2026-08-11 05:07:04.779838072 +0000
@@ -2,7 +2,7 @@
 setlocal
 cd /d "%~dp0"
 echo ==========================================
-echo   LunaxyMusic V38 - Debug APK Build
+echo   LunaxyMusic V39 - Debug APK Build
 echo ==========================================
 call gradlew.bat :app:assembleDebug
 if errorlevel 1 (
@@ -12,7 +12,7 @@
   exit /b 1
 )
 if not exist dist mkdir dist
-copy /Y "app\build\outputs\apk\debug\app-debug.apk" "dist\LunaxyMusic-V38-debug.apk" >nul
+copy /Y "app\build\outputs\apk\debug\app-debug.apk" "dist\LunaxyMusic-V39-debug.apk" >nul
 echo.
-echo [OK] APK: %CD%\dist\LunaxyMusic-V38-debug.apk
+echo [OK] APK: %CD%\dist\LunaxyMusic-V39-debug.apk
 pause
diff -ruN /mnt/data/v39diff/old/app/build.gradle.kts /mnt/data/v39diff/new/app/build.gradle.kts
--- /mnt/data/v39diff/old/app/build.gradle.kts	2026-08-11 05:07:04.775195927 +0000
+++ /mnt/data/v39diff/new/app/build.gradle.kts	2026-08-11 05:07:04.776178957 +0000
@@ -10,8 +10,8 @@
         applicationId = "com.xingyu.music"
         minSdk = 26
         targetSdk = 36
-        versionCode = 41
-        versionName = "38.0.0"
+        versionCode = 42
+        versionName = "39.0.0"
     }
 
     buildTypes {
diff -ruN /mnt/data/v39diff/old/app/src/main/java/com/xingyu/music/data/ResolverHttpDiagnostics.java /mnt/data/v39diff/new/app/src/main/java/com/xingyu/music/data/ResolverHttpDiagnostics.java
--- /mnt/data/v39diff/old/app/src/main/java/com/xingyu/music/data/ResolverHttpDiagnostics.java	2026-08-11 05:07:04.771605347 +0000
+++ /mnt/data/v39diff/new/app/src/main/java/com/xingyu/music/data/ResolverHttpDiagnostics.java	2026-08-11 05:07:04.772410028 +0000
@@ -13,16 +13,20 @@
 /**
  * Observation-only HTTP trace for the bundled Huibq endpoint.
  *
- * This class never performs a request, never retries, never mutates source health,
- * and never changes a response. It only summarizes a response that Http has already
- * received so a later "unknow error" can be diagnosed without changing V14 routing.
+ * V39 safety rules:
+ * - never performs/retries a request;
+ * - never mutates routing, source health, cache, or response data;
+ * - inspects only a small fixed prefix of an already-completed response body;
+ * - keeps a small fixed in-memory ring so diagnostics cannot grow without bound.
  */
 public final class ResolverHttpDiagnostics {
-    private static final int MAX = 12;
+    private static final int MAX_LINES = 8;
+    private static final int MAX_BODY_PREFIX_CHARS = 384;
+    private static final int MAX_LINE_CHARS = 360;
     private static final List<String> lines = new ArrayList<>();
     private static final String HUIBQ_HOST = "lxmusicapi.onrender.com";
     private static final Pattern CODE = Pattern.compile("[\\\"']?code[\\\"']?\\s*:\\s*([\\\"']?[^,}\\s\\\"']+[\\\"']?)", Pattern.CASE_INSENSITIVE);
-    private static final Pattern MSG = Pattern.compile("[\\\"']?msg[\\\"']?\\s*:\\s*[\\\"']([^\\\"']{0,160})[\\\"']", Pattern.CASE_INSENSITIVE);
+    private static final Pattern MSG = Pattern.compile("[\\\"']?msg[\\\"']?\\s*:\\s*[\\\"']([^\\\"']{0,120})[\\\"']", Pattern.CASE_INSENSITIVE);
 
     private ResolverHttpDiagnostics() { }
 
@@ -30,12 +34,18 @@
         try {
             if (response == null || !isHuibq(url)) return;
             String contentType = header(response.headers, "Content-Type");
-            String body = summarizeBody(response.body);
-            StringBuilder detail = new StringBuilder();
+            String contentLength = header(response.headers, "Content-Length");
+            String raw = response.body == null ? "" : response.body;
+            int bodyChars = raw.length(); // O(1), does not scan/copy the full body.
+            String prefix = raw.substring(0, Math.min(bodyChars, MAX_BODY_PREFIX_CHARS));
+
+            StringBuilder detail = new StringBuilder(256);
             detail.append("Huibq HTTP ").append(response.code);
-            if (!contentType.isEmpty()) detail.append(" · ").append(shorten(contentType, 72));
-            if (!body.isEmpty()) detail.append(" · ").append(body);
-            add(detail.toString());
+            if (!contentType.isEmpty()) detail.append(" · type=").append(shorten(contentType, 52));
+            if (!contentLength.isEmpty()) detail.append(" · len=").append(shorten(contentLength, 20));
+            detail.append(" · chars=").append(bodyChars);
+            detail.append(" · ").append(summarizePrefix(prefix, bodyChars));
+            add(shorten(detail.toString(), MAX_LINE_CHARS));
         } catch (Exception ignored) {
             // Diagnostics must be impossible to turn into a playback failure.
         }
@@ -43,14 +53,14 @@
 
     public static synchronized String text() {
         if (lines.isEmpty()) return "";
-        StringBuilder b = new StringBuilder();
-        b.append("--- Huibq HTTP 纯观察（不参与选路/学习） ---");
+        StringBuilder b = new StringBuilder(3200);
+        b.append("--- Huibq HTTP 纯观察（固定长度，不参与选路/学习） ---");
         for (String line : lines) b.append('\n').append(line);
         return b.toString();
     }
 
     private static synchronized void add(String detail) {
-        if (lines.size() >= MAX) lines.remove(0);
+        if (lines.size() >= MAX_LINES) lines.remove(0);
         lines.add(time() + "  " + detail);
     }
 
@@ -72,21 +82,26 @@
         return "";
     }
 
-    private static String summarizeBody(String raw) {
-        String t = raw == null ? "" : raw.trim();
-        if (t.isEmpty()) return "body=empty";
+    /** Only the bounded prefix reaches regex/whitespace processing. */
+    private static String summarizePrefix(String prefix, int fullChars) {
+        String t = safe(prefix).trim();
+        if (fullChars == 0 || t.isEmpty()) return "body=empty";
         String oneLine = t.replaceAll("\\s+", " ");
         boolean jsonLike = oneLine.startsWith("{") || oneLine.startsWith("[");
+        String suffix = fullChars > prefix.length() ? "…[truncated]" : "";
         if (jsonLike) {
             String code = match(CODE, oneLine);
             String msg = match(MSG, oneLine);
             StringBuilder b = new StringBuilder("body=json");
-            if (!code.isEmpty()) b.append(" code=").append(shorten(stripQuotes(code), 32));
-            if (!msg.isEmpty()) b.append(" msg=").append(shorten(msg, 96));
-            if (code.isEmpty() && msg.isEmpty()) b.append(" snippet=\"").append(shorten(redactUrl(oneLine), 120)).append("\"");
+            if (!code.isEmpty()) b.append(" code=").append(shorten(stripQuotes(code), 24));
+            if (!msg.isEmpty()) b.append(" msg=").append(shorten(msg, 72));
+            if (code.isEmpty() && msg.isEmpty()) {
+                b.append(" snippet=\"").append(shorten(redactUrl(oneLine), 96)).append("\"");
+            }
+            b.append(suffix);
             return b.toString();
         }
-        return "body=text \"" + shorten(redactUrl(oneLine), 140) + "\"";
+        return "body=text \"" + shorten(redactUrl(oneLine), 104) + suffix + "\"";
     }
 
     private static String match(Pattern p, String s) {
@@ -103,7 +118,7 @@
     }
 
     private static String redactUrl(String s) {
-        // Never surface a playable media URL in diagnostics. Service/error URLs are reduced to a marker.
+        // Operates only on the <=384-char prefix; never scans the full response body.
         return safe(s).replaceAll("https?://[^\\s\\\"']+", "<url-redacted>");
     }
 
