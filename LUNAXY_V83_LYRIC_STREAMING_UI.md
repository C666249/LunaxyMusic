# Lunaxy Music V83 — 歌词流式增量渲染

基线：Stable V82。

本轮只优化搜索页歌词结果的 UI 调度，不修改网易/QQ歌词接口、播放、下载、Provider、Canonical Track 等稳定链路。

## 核心改动

1. 歌词异步结果不再触发整个搜索结果页 `removeAllViews()` 重建；仅更新“歌词匹配”区域。
2. 第一批歌词候选最多先绘制 3 条，后续每约 118ms 追加最多 3 条，形成渐进式流式呈现，避免一次创建 20+ 卡片。
3. 第一条真实 LRC 精确命中立即刷新；其余并发核验结果以约 96ms 窗口合批刷新，避免 12 个线程连续轰炸主线程。
4. 已经创建的歌词卡片复用，只更新歌名、歌手、命中片段、封面与点击绑定；新卡片才执行轻量淡入上移动画。
5. 搜索最终的歌词身份补全/重排移到既有 `searchMergeExecutor` 后台执行，避免在四平台首轮搜索收口时把 ExactTrackMatcher 批量计算压到主线程。
6. 本地收藏/歌单/历史歌词扫描仍为 0；V82 已修复的独立 lyric executor 状态机保持不变。

## 预期体验

- 第一条/第一批歌词命中尽快出现；
- 后续结果分批长出来，而不是整页冻结后一次跳出；
- 搜索 Tab、平台筛选和普通歌曲区在歌词流式更新期间保持稳定，不重复销毁创建；
- 主线程只承担小范围 View 更新，显著降低几秒级页面无响应风险。
