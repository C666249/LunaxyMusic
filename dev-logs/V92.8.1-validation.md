# LunaxyMusic V92.8.1 验证记录

## 基线与修改边界

- 明确以已交付 V92.8 Stable/Beta 完整源码为基线。
- `MainActivity.java` 与 V92.8 功能 diff 仅包含：3 个动画时长常量、450→900 ms、260→520 ms、220→440 ms。
- `PlayerSurfaceMorphView.java`、`ArtworkMorphView.java`、`SpringMotion.PLAYER_OPEN` 未改。
- 播放、推荐、搜索、缓存冻结核心与 V92.8 哈希一致。

## 已完成验证

- MotionRegressionTest：223 项通过。
- Player926PolicyTest：7 项通过。
- VinylGesturePolicyTest：7 项通过。
- Stable：17 个 Android XML 全部可解析。
- Beta：17 个 Android XML 全部可解析。
- Stable/Beta `app/src/main/java` 功能 Java 文件逐文件 SHA256 一致。
- Stable applicationId/version：`com.xingyu.music` / `92.8.1` / 944。
- Beta applicationId/version：`com.xingyu.music.beta1` / `92.8.1-beta37` / 945；App 名带 Beta，启动图标保持 β。
- 动画时长静态审计：点击 900 ms；手势打开收束 520 ms；手势回退 440 ms。

## 播放解耦审计

`ValueAnimator` 更新回调只触碰 Surface/Artwork/Player View 的 geometry/alpha/scale/translation/reveal；未调用播放 seek、play/pause、skip、queue mutation 或 PlaybackService 调度方法。因此动画放慢不会改变音频播放速度或播放进度。

## Android 真编译边界

尝试执行 `./gradlew --offline :app:assembleDebug`。项目 wrapper 本地没有 Gradle 9.5.0 分发缓存，仍尝试访问 `services.gradle.org`；当前执行环境 DNS 无法解析该主机，因此停留在 Gradle 分发获取阶段，未进入 AGP/Android Java 编译。

本轮不宣称 Android 真编译通过。请在联网或已有 Gradle 9.5.0 + Android SDK 36 缓存的 Android Studio 环境直接打开项目根目录执行 `:app:assembleDebug`。
