# V92.8.3 Validation

## 目标
消除 Mini Player 点击进入 Full Player 时 Hero 代理唱片与目标真实唱片同时可见的“双层”错觉。

## 代码级检查
- Hero update loop 中真实 `nowVinyl` 始终 `alpha = 0`。
- Hero proxy 在飞行期保持 `alpha = 1`，不在抵达前提前淡出。
- `onAnimationEnd` 先恢复真实唱片 `alpha = 1`，再移除 Hero proxy。
- `cancelPlayerOpenMotion()` 恢复真实唱片 alpha，防止取消态残留。
- 450–1000 ms 动画时长设置与 PlaybackService 仍无耦合。

## 自动检查
- MotionRegressionTest：PASS，223 checks。
- Player926PolicyTest：PASS，7 checks。
- VinylGesturePolicyTest：PASS，7 checks。
- Android XML：全部可解析。
- Stable/Beta `app/src/main/java`：逐文件 SHA256 同源检查。
- Hero handoff 策略：检查占用锁启用/解除、真实唱片 alpha、Hero proxy 全程不提前淡出。
- ZIP：生成后执行 `unzip -t` 完整性检查。

## Android 构建边界
当前执行环境无法解析/访问 `services.gradle.org` 下载缺失的 Gradle 9.5.0，因此不能在此环境完成 Android Gradle 真编译；不将静态/离线检查描述为 APK 编译成功。
