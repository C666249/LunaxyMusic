# Gradle build attempt

The sandbox attempted the Android Gradle build, but the repository's wrapper/bootstrap needs Gradle 9.5.0 and the environment cannot resolve `services.gradle.org`. The failure happens before AGP compilation starts. This is an infrastructure/network limitation, not reported as a successful build.

Observed error: `curl: (6) Could not resolve host: services.gradle.org`.
