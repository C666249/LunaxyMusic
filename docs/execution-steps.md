# V92.4 execution

1. Read baseline and latest requirements; verify the unchanged baseline compiles (passed).
2. Add regressions before changing state/geometry; implement the scoped UI fixes.
3. Run model tests, Android compilation and lint; inspect emulator/device if available.
4. Audit frozen core and Stable/Beta identities, create checksummed full source ZIPs, document actual validation limits.
