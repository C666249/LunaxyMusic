# V92.4 visual constraints

Preserve typography, controls, cover palette and existing starfield. Center record scale 1, alpha 1; near record scale .88, alpha .35; far record scale .76, alpha .15. Mirror left/right. Prefer shallow overlap to three independent panels. No rectangular clipping inside the playback viewport. Particles span the stack with restrained foreground points. Hero/Mini transitions target the actual center artwork circle.
