# Lunaxy Music V32.0

本版基于 V31，重点是大歌单继续加载体验、全局歌曲下载、歌单内单曲删除，以及歌单 Tab 分类。Huibq / 全豆要的网络播放主链继续冻结。

## 1. 大歌单继续加载不再跳回顶部

歌单详情仍按 40 首分段创建视图，避免 700~800 首歌一次性渲染造成卡顿。点击“继续加载 40 首”时会记住当前 ScrollView 的实际 Y 位置，关闭本次页面切换动画，重建后恢复到原位置。新 40 首直接出现在当前位置下方，可以继续向下滑。

## 2. 所有歌曲“三个点”统一增加下载

搜索结果、最近播放、每日推荐、私人电台、天气电台、收藏、任意歌单以及正在播放页的歌曲更多菜单均可下载。

下载优先使用 PlaybackUrlStore 中已被 Media3 READY 验证过的 URL；没有可用缓存时，使用新增的 DownloadSourceResolver 独立解析。该解析器不读写 SourceHealthStore，因此不会改变现有音源学习、路由排序或冷却状态。

Android 10+ 下载写入系统 MediaStore 的 `Music/Lunaxy Music`，并标记为音乐/铃声音频；V32 的“离线”列表直接播放这个本地 URI，不再请求 Huibq/QDY。Android 9 及更早版本使用 App 自有 Music 目录作为兼容回退。

## 3. 歌单内“三个点”额外增加删除

只有从某个真实歌单详情（含该歌单内搜索结果）打开歌曲更多菜单时，最底部额外显示“从该歌单删除”。其它页面不会出现这一项。

## 4. 歌单 Tab 分类

歌单页分成三组：

- 我的歌单：本机创建、合并后的 local 歌单
- 导入歌单：从 QQ / 网易等平台导入并保存在本机的歌单
- 离线：已下载歌曲，点击“离线歌曲”进入与普通歌单一致的详情列表

每组右侧有三角展开键，带旋转 + 内容淡入/收起动画；展开状态会保存在 UI 设置中。

## 5. 播放保护

`PlaybackService / MultiSourceManager / SourceHealthStore / PlaybackUrlStore / LxSourceRuntime / Huibq JS / QQMusicApi / NeteaseApi` 等 15 个网络播放核心对象与 V31 SHA-256 完全一致。

仅有意对 `SourceCoordinator` 增加 `local` 离线 URI 直通分支，对 `Song.providerLabel` 增加“离线”标签；本地离线分支不会写音源健康度。

详见 `changes/PLAYBACK_CORE_GUARD_V32.txt`。
