# Lunaxy Music V26.0

基线：XingyuMusic V25-Full。

## 本版改动

- 应用显示名称改为 **Lunaxy Music**，Android Studio 工程名同步为 `LunaxyMusic`；包名 `com.xingyu.music` **刻意保持不变**，避免覆盖安装后丢失历史、收藏、歌单、音源健康学习与已验证 URL 缓存。
- 当前启动图标/圆形图标资源保持原样，未重新设计或替换。`BUILD_APK.bat` 的输出文件名同步改为 `LunaxyMusic-V26-debug.apk`。
- Android 13+ 接入系统 `OnBackInvokedCallback`，旧系统继续走 `onBackPressed()`，两者统一进入同一套层级返回逻辑：临时弹层/播放列表 → 播放详情 → 推荐详情/歌单详情 → 根 Tab → 首页；首页再返回仅把任务放到后台。
- 歌单详情返回固定回到「歌单」Tab；展开播放列表后系统返回只收起播放列表。
- 歌单详情删除「从第一首播放」大卡片。搜索、添加歌曲、批量管理三个按钮集中到右上角一排；直接点第一首本来就会把整个歌单设为播放列表并从第一首播放。
- 向歌单新增的单曲/批量歌曲放到歌单顶部；已经存在的歌曲不重复、不改变原位置。
- 歌单列表原删除按钮换为三点菜单，提供「重命名 / 删除」。
- 首页原“星雨音乐 · 找到想听的，直接播放”改为随机歌词单行：只从收藏和已保存歌单的歌曲取歌词，约每 3 秒淡入淡出切换。优先使用本地歌词缓存，必要时最多后台补取少量歌词；不调用 Huibq/QDY，不进入音频播放解析链。
- “为你推荐”右侧的“本地口味 · 天气与时间”已删除。

## 播放链保护

音源解析、Huibq/QDY 调度、Media3 READY 判定、SourceHealthStore、PlaybackUrlStore、失败冷却与回退逻辑均未修改。`PlaybackService.java` 仅把没有当前歌曲时的通知标题从旧品牌名改为 `Lunaxy Music`，完整归一化 diff 已验证除这一个字符串外完全一致。

详细核验见 `changes/PLAYBACK_CORE_GUARD_V26.txt`，完整代码差异见 `changes/V25-to-V26_UI_NAV_PLAYLIST.patch`。
