# V92.4 — 唱片堆叠与队列连续性

基于用户提供的 V92.3 Beta 原始源码制作，Stable/Beta 使用相同功能源码。

## 播放详情

- 当前唱片、前后各两张唱片共享连续空间；近层尺寸 88%、透明度 35%，远层尺寸 76%、透明度 15%。左右只露出边缘。
- 解除内容列、滚动容器、安全区域内部的裁剪，边缘不再形成分页式竖直切口。
- 位移、缩放、透明度、景深跟随手指；松手依据距离/速度吸附。移除慢拖超过 1.1 秒强制取消的旧限制。
- 采用五个保留的唱片 View 交换角色。拖到中央的唱片继续作为当前唱片，避免交叉淡入时重新生成中央层。
- 顺序模式的预览与队列首尾循环一致。随机模式保持播放引擎的随机选歌语义，不展示会被误认为确定下一首的封面预告。
- 粒子分处唱片间和唱片前方。Android 12+ 使用真实模糊；Android 8–11 保留缩放、透明度及前后层级。

## 播放队列与手动排序

- 流体框的歌曲身份、颜色插值、播放/暂停动画时钟由歌曲状态持有，View 回收或落位重绑定不会重置时钟。
- 拖动当前歌曲时，浮层使用实时流体框，而非长按开始时冻结的截图边框。
- 落位在实际新布局的 OnPreDraw 阶段完成状态交接，去掉依赖两次“下一帧回调”的猜测性时序。
- 去掉当前行额外截图补偿，避免半透明边框重复叠加引起亮度闪动；新布局不再继承旧位置的位移或隐藏状态。
- 播放/暂停状态更新会同步到流体框；重复绑定相同封面颜色不会重启颜色过渡。

## 额外修正

- Hero 展开过程中返回：取消旧动画与待执行回调，防止播放页再次弹出。
- Mini Player 展开：固定起止坐标，以真实封面圆直径作为缩放基准，消除动画坐标反复参与计算造成的漂移。
- 歌曲切换中返回：取消待提交动画，过期回调不能偷偷再切歌。
- 横向手势与纵向滚动互斥，左右系统返回边缘保留；歌词区继续保持独立手势。

播放引擎、Provider、推荐、搜索、封面/歌词缓存和数据存储功能源码均保持原版，详细文件审计见验证文档。

## 安装与验收

提供的是本机 debug 签名 APK。Stable 包名 `com.xingyu.music`；Beta 包名 `com.xingyu.music.beta1`，保留 β 图标与独立数据。
覆盖升级需与已安装版本签名一致。如 Android 报签名冲突，不应卸载含重要数据的旧版；可用原签名环境从源码构建。

建议真机复查：暂停/播放状态下拖动当前曲和跨过当前曲；长拖到列表两端再松手；慢拖、反向回拉、首尾切歌；Mini 展开和 Hero 中途返回。模拟器通过不替代不同 OEM、Android 8–11 或 90/120Hz 设备的最终视觉验收。
