# Lunaxy Music V87 — Intelligence Surface + Search Engine V2

## 版本定位

V87 在 V86 `Personalization Engine V2 + ListenBrainz` 的基础上完成两条产品化主线：

1. 把原本只在后台工作的 Session / 7天 / 30天 / 180天画像、推荐构成和推荐原因搬到前台，形成可视化的「个性化矩阵」。
2. 把搜索从“四源同时重压 + 普通关键词也跑歌词 + 结果区反复整体重建”升级为渐进式 `Search Engine V2`。

本轮继续遵守播放平面隔离红线：推荐和搜索只决定“找什么/展示什么”，不改变已经稳定的音频实时解析和失效换链机制。

---

## 1. Personalization Engine V2.1：真正可验证的“新发现”

### 1.1 未听过判定升级

V86 已能从熟悉歌手扩展和 ListenBrainz 协同关系中召回陌生候选。V87 进一步修正“没听过”的判定：

- 收藏、歌单、最近播放仍然纳入已知集合；
- 同时读取 `PersonalizationStore.track_stats` 长期歌曲账本；
- 不再只使用 `snapshot()` 中最近 1200 首歌曲，因此旧歌曲不会因为掉出分析快照就被误标为“第一次发现”。

这里的“未听过”严格表示：**在 Lunaxy 当前可追溯的本机行为历史中，没有这条 canonical track key 的播放/行为记录**。它不声称知道用户在其他 App、其他设备或 V86 行为数据库建立之前的全部人生听歌历史。

### 1.2 日推与私人电台的探索配额

候选足够时：

- 每日推荐：目标至少约 55% 为 Lunaxy 历史未听过候选；
- 私人电台：目标至少约 25% 为未听过候选，同时保留更多当前 Session / 熟悉内容作为连续播放锚点；
- 如果在线目录/ListenBrainz 当次无法提供足够合格陌生候选，不伪造比例，自动由高质量熟悉内容补齐。

最终前台展示的是**实际生成结果的真实比例**，而不是固定宣传数字。

### 1.3 推荐原因固化

每次生成推荐时，同时保存每首歌对应的：

- origin：ListenBrainz / familiar / rediscovery / local / weather 等；
- why：例如“相似听众发现 · 你还没听过”“最近兴趣 · 喜欢歌手的新发现”等；
- unheard：当次排序时的真实未听过判断；
- score：内部排序分值快照。

因此页面重新打开后，“为什么推荐”和“新发现比例”不会因为重新计算而漂移。

---

## 2. 个性化矩阵 / Intelligence Surface

首页推荐区域新增「个性化矩阵」入口，采用与播放链矩阵一致的“透明引擎”产品语言。

### 概览

- 学习成熟度；
- 已学习歌曲数量；
- 当前 Session 的有效听完 / 跳过 / 重播 / 收藏；
- 当前最强兴趣及趋势提示。

### 画像

按歌手显示四个时间尺度的前台归一化指数：

- Session：此刻偏好；
- 7天：近期口味；
- 30天：中期趋势；
- 180天：稳定长期趋势。

指数只用于可视化，不直接暴露内部工程权重。

### 推荐

分别显示「每日推荐」和「私人电台」的实际构成：

- 新发现占比；
- ListenBrainz 协同发现数量；
- 熟悉歌手扩展数量；
- Rediscovery 数量。

### 数据

明确展示本地行为数据库的作用与隐私边界：

- 长期 Track Stats；
- 有界近期原始事件；
- 7/30/180 天日聚合；
- ListenBrainz 只读协同发现，不上传 Lunaxy 听歌历史；
- 推荐数据不保存音频播放 URL / READY 路由。

### 单曲“为什么推荐”

在日推 / 私人电台 / 天气电台的歌曲操作菜单中新增「为什么推荐这首」。推荐合集顶部也可查看整批推荐构成。

---

## 3. Search Engine V2

### 3.1 Stale-While-Revalidate 元数据缓存

新增 `SearchCacheStore`：

- 最近 32 个搜索词；
- 每个最多保存 36 条 canonical Song 元数据；
- TTL 24 小时；
- 第二次搜索相同关键词时先从本机缓存瞬时显示，再在后台刷新全部来源；
- 超出上限按 LRU 淘汰。

该缓存属于**搜索展示/目录身份缓存**，不读取或写入 `PlaybackUrlStore`，不缓存 SourceCoordinator 已解析的临时音频 URL。封面 URL 属于展示元数据，仍由 V85 的独立图片磁盘缓存负责实际图片文件复用。

### 3.2 自适应两波 Provider 调度

新增 `SearchPerformanceStore`，只用于搜索：

- 记录网易 / QQ / 酷我 / 酷狗最近搜索延迟 EMA、成功率和结果量；
- 下一次优先启动近期表现最好的 2 个来源；
- 其余来源约 420ms 后补齐；
- 首屏每来源从 30 条收敛到 20 条，完整结果继续通过原分页机制加载；
- 某来源近期变慢/失败率升高，会自动掉到第二波；不会影响播放路线的 Provider 健康状态。

### 3.3 搜索请求生命周期

- 新关键词开始时取消上一轮仍在执行的搜索 Future；
- 搜索建议也取消旧网络任务；
- 最近搜索匹配结果先本地立即显示，再延迟拉取网易/QQ建议补全；
- Activity 销毁时取消搜索/建议/延迟补全任务，避免后台残留工作。

HTTP 底层仍沿用现有稳定 Provider 适配器，本轮没有为追求“架构漂亮”而重写所有网络客户端，以降低播放/下载/其他既有 API 链路回归风险。

### 3.4 意图驱动歌词搜索

V86 以前 `>=3` 字符的普通搜索很容易同时启动歌词索引和歌词验证。V87 改为：

- 明显歌词句子：立即启动歌词链；
- 混合型关键词：先给歌名/歌手结果约 900ms 的机会，只有元数据结果弱时再自动启动歌词；
- 普通歌名/歌手关键词：不启动歌词链；
- 用户主动点击「歌词」Tab：立即按需启动歌词搜索。

歌词搜索仍使用原 V83 流式索引/验证链，没有重写其数据源实现。

### 3.5 持久 Search Shell，减少反复重建

原搜索每个 Provider 返回后会对结果 Holder 执行 `removeAllViews()` 并重建 Tab、来源筛选和内容。V87 调整为：

- 综合 / 单曲 / 歌词 Tab Shell 只创建一次；
- 来源筛选区域独立刷新；
- 只有结果主体更新；
- 对结果 key + variant 数量 +歌词验证状态生成轻量 UI signature，内容没有实质变化时不重新创建歌曲卡；
- canonical variant 真正增加时仍会刷新对应内容，确保点击歌曲拿到最新多来源身份，不为了少刷新牺牲播放候选完整性。

本轮保持现有轻量 Java UI 技术栈，没有为了 DiffUtil 额外引入 RecyclerView/AndroidX 依赖；后续如果真机 Trace 显示歌曲卡本身 inflation 仍是主要瓶颈，再独立迁移列表容器。

### 3.6 搜索矩阵

搜索来源 Chip 行新增「矩阵」入口，可查看：

- 本机缓存首帧耗时；
- 第一份网络结果耗时；
- 初始四源稳定耗时；
- 当前搜索意图；
- 是否启动歌词链；
- 每个 Provider 本次耗时；
- 每个 Provider 学习后的 EMA 延迟、成功率和样本数。

这个页面用于区分“网络源慢”和“UI 合并/渲染慢”，后续可以直接拿截图做性能定位。

---

## 4. 播放链冻结审计

V87 不修改以下核心文件：

- `PlaybackService.java`
- `SourceCoordinator.java`
- `PlaybackUrlStore.java`

同时 V85 的：

- `ImageLoader.java` 封面磁盘缓存；
- `LyricCacheStore.java` 歌词缓存；

也保持 V86 字节级不变。

推荐/搜索缓存与播放 URL 生命周期继续完全分层；临时播放链过期时仍由原 PlaybackUrlStore / SourceCoordinator 自愈重新解析。

---

## 5. V87 代码变更边界

相对 V86，核心运行时代码集中在：

- `MainActivity.java`
- `RecommendationEngine.java`
- `PersonalizationStore.java`
- `TasteProfileEngine.java`
- 新增 `SearchCacheStore.java`
- 新增 `SearchPerformanceStore.java`

没有为了 V87 改动 Provider 播放解析核心。
