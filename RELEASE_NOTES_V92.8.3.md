# V92.8.3 — Hero 唱片单层交接修复

本轮继续以 V92.8.2 / V92.8 动画体系为基线，不改变动画路径、星空实验室时长设置或播放逻辑，只修复 Mini Player 点击进入播放详情时的“双层唱片”视觉问题。

## 修复
- Hero 代理唱片飞行期间，播放详情中的真实中心唱片保持完全隐藏。
- 不再从动画约 50% 处提前渐显真实唱片，也不再让 Hero 代理在末段提前淡出。
- Hero 到达最终几何位置后，在动画结束回调中先让真实唱片接管，再移除 Hero 代理，形成单帧无缝 handoff。
- 动画被取消/返回/重入时会恢复真实唱片 alpha，避免异常中断后唱片保持隐藏。

## 保持不变
- 星空实验室中的 450–1000 ms 播放器展开动画时长设置保持原样，默认 900 ms。
- PlaybackService、播放进度、seek、切歌、队列和音源链路与动画时间完全解耦。
- Mini Player 上滑直接操控路径保持原逻辑；该路径使用真实唱片本体变换，不存在重复 Hero 层。
