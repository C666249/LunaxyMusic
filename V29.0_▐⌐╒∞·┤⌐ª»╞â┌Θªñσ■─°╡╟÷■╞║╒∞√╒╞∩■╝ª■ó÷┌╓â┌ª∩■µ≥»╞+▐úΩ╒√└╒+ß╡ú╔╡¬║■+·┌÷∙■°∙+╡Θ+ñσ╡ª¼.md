# Lunaxy Music V29.0

本版基于 V28，只处理歌词配色 UI 与「添加歌曲」选择器性能，不改播放/音源核心。

## 1. 歌词高亮选择器
- 冰蓝 / 星紫 / 薄荷 / 日落 / 玫瑰 / 银白：每一行现在直接显示完整渐变高亮，不再只看到灰白基础字色。
- 「自定义两端颜色」改为「自定义渐变颜色」，选项页本身可滚动，不再出现底部文字被截半。
- 不再要求手输 HEX：新增原生 HSV 选色器，上方为饱和度/明度区，下方为色相条。
- 选好颜色后可一键设为渐变起点或终点，并在顶部实时预览，最后点「应用渐变」。自定义页内容可滚动，应用键固定在底部。
- 原来的「跟随歌曲封面颜色」继续保留。

## 2. 添加歌曲性能
旧版卡顿原因主要不是“歌单数据本身太大”，而是 UI 采用 ScrollView + LinearLayout，一进入几百首歌单就一次性创建大量歌曲行、封面 ImageView、CheckBox，并触发大量封面加载；全选后还会把整个歌曲页面重新构建一次。

V29 改为：
- 歌曲选择列表使用 ListView + ViewHolder 复用，只创建屏幕附近可见的少量 Row。
- 取消旧的“最多显示前 160 首”临时限制，几百首也可完整滚动查看。
- 收藏 / 最近播放 / 搜索歌曲 / 其他歌单 四个 Tab 在一次弹窗会话内缓存，来回切换不重复重建整个页面。
- 「其他歌单」内全选/取消全选改为 O(n) 更新选择集合 + adapter.notifyDataSetChanged()，不再重建数百个歌曲 Row。
- 目标歌单已有歌曲使用一次性 key 集合判断，避免对大歌单反复线性扫描。

## 3. 播放链保护
V28 → V29 对 21 个保护对象逐个计算 SHA-256，21/21 完全一致。详见：
`changes/PLAYBACK_CORE_GUARD_V29.txt`

完整代码差异：
`changes/V28-to-V29_UI_PERFORMANCE.patch`
