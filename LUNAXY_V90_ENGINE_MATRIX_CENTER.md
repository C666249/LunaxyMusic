# Lunaxy Music V90 — 引擎矩阵中心

本轮目标：把 V89 分散的播放链/个性化/搜索矩阵入口收敛成统一的高级矩阵中心。

## 交互
- 首页删除常驻“智能音源”大卡，让最近播放与“为你推荐”更早露出。
- 首页标题右上角新增统一 SCAN 引擎键。
- 进入完整矩阵中心后，顶部在“播放链 / 个性化 / 搜索”三页之间切换。
- 顶部横向区域支持点击与左右滑动；切换使用短距离平移 + 淡入淡出，避免整页重建闪烁。
- 原搜索矩阵的窄关闭条升级为 52dp 全宽底部操作条。
- 系统返回键遵循铁律：矩阵只退回上一级页面，不退出 App。

## 安全边界
- 不修改 PlaybackService、SourceCoordinator、PlaybackUrlStore。
- 不修改 V88/V89 推荐与 Search Engine V2 的算法、缓存和网络调度。
- 本轮运行时代码仅修改 MainActivity 的展示和导航层。
